// MonthSidebar is no longer used.
// All controls are now in the horizontal toolbar inside TimelineGrid.
// This file is kept as a stub to avoid import errors during migration.

export default function MonthSidebar() {
  return null;
}